### Task 3: User App - Foundation & Browsing
Implement the basic user flows in `cinebook_user_app`:
1. OTP login screen (wire to `AuthBloc`, endpoints: `/auth/request-otp`, `/auth/verify-otp`).
2. Movie list screen (`GET /movies?...filters` with basic filter sheet).
3. Movie detail screen (`GET /movies/:id`, `/reviews`, `/similar`).
4. Set up bottom navigation or tab structure, leaving a placeholder tab for the future AI Chatbot (Phase 4).

